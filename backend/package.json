{
  "name": "gasnet-backend",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "test": "vitest --globals",
    "test:watch": "vitest --watch --globals",
    "test:unit": "vitest run --globals",
    "lint": "eslint ."
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "eslint": "^10.5.0",
    "supertest": "^6.3.3",
    "vitest": "^4.1.9"
  }
}
