# Note for instructions.

The project is GasNet.

## Note this for language.
We will be using Javascript for Backend (ExpressJs) and Frontend (ReactJs). 

## You will be using the following command for testing packages
```
npm install vitest @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom --save-dev
```

## Strictly do this

As of now, there will be only mock data. And to represent testing, there will be mock data for testing. You will need to the following in the folder of requirements to do what is need to be done. Strictly follow it. Every thing should have a test using the tesing packages mentioned above. 